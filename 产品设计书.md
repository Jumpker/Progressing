# 产品设计书：Progressing（每日进度条）

| 项目 | 内容 |
| --- | --- |
| 产品名 | Progressing |
| 中文名 | 每日进度条 |
| 版本 | v0.5 |
| 日期 | 2026-08-09 |
| 状态 | 待评审 |
| 目标平台 | Windows（10 / 11） |

---

## 1. 产品概述

**一句话**：屏幕上一条细长的、置顶显示的进度条，代表今天 24 小时，指针随时间移动，用户可把一天中的习惯时间段标注为彩色段并附上文案。

**产品形态**：常驻系统托盘的轻量小工具，包含三个部分——

1. **进度条**：屏幕上置顶显示、鼠标穿透的细长条，支持多条并存；
2. **托盘图标**：唯一的配置入口；
3. **设置窗口**：所有配置都在这里完成。

---

## 2. 核心概念模型

| 概念 | 定义 |
| --- | --- |
| **进度条（实例）** | 一条代表"今天 24 小时"的线，长度与 24 小时等比例映射。可创建多条，各自独立命名、配置、显示/隐藏。 |
| **指针** | 标记当前时刻的图标，在进度条上随时间平滑移动。 |
| **时间段备注** | 用户在 24 小时轴上划出的一段区间（起止 hh:mm + 文案 + 颜色）。同一条进度条上时间段互不重叠。 |
| **色池** | 时间段彩色段的取色来源，内置一套互相搭配的柔和色系；用户可自定义扩充。 |

---

## 3. 功能需求

### 3.1 进度条本体

#### 3.1.1 形态与尺寸

- 默认形状：细长条，两端圆头（胶囊形）；未标注任何时间段时为灰色。
- 默认尺寸：横放长 600px、宽 4px；长度可调 200 ~ 2000px，宽度可调 2 ~ 10px（步进 1px）。
- **时间等比例映射**：进度条全长恒等于 24 小时（0:00 ~ 24:00）。调整长度/宽度时，任意时刻与时间段的映射位置自动按比例缩放，时间段之间的相对比例关系保持不变。

#### 3.1.2 方向

- **横放**：从左到右代表 0:00 → 24:00。
- **竖放**：从上到下代表 0:00 → 24:00。

#### 3.1.3 镜像

- 提供"镜像"开关：以进度条中心为对称轴翻转（横放左右翻转，竖放上下翻转）。
- 翻转后时间递增方向同步反转：
  - 横放镜像：0:00 在最右，时间从右往左走；
  - 竖放镜像：0:00 在最下，时间从下往上走。
- 指针走向同步反转；时间段映射、起止时间标注等与时间位置相关的呈现整体镜像。备注文字的基准方向设置（上方/下方/左侧/右侧）不受镜像影响。
- 目的：适应不同屏幕布局与个人习惯。

#### 3.1.4 边框

- 可选开启边框，边框颜色可自定义；默认关闭（保持极简）。
- 边框宽度默认 1px。

#### 3.1.5 位置

进度条整体**鼠标穿透，不可直接拖动**。位置通过两种方式配置：

1. **数值/预设**（设置窗口 → 位置）：预设（屏幕顶部居中/底部居中/左侧居中/右侧居中，可指定目标显示器）；自定义坐标 X/Y；微调按钮（每步 1px，按住连跳 10px）。
2. **拖动编辑模式**：点击"进入位置编辑模式"后，进度条临时取消鼠标穿透，并**强制以不透明状态显示**（不受透明度设置影响）且带高亮轮廓，**可直接鼠标拖拽**到任意位置；此时**备注文字强制显示**并提供拖拽手柄（无论指针是否位于时间段内），可拖拽调整其相对进度条的偏移。按 Esc 或点击"完成"退出，恢复鼠标穿透并保存。

#### 3.1.6 置顶

- 提供"是否置顶"开关：开启后进度条始终显示在其他窗口之上（默认开启）；关闭后进度条像普通窗口一样可被覆盖，仅保留鼠标穿透。

#### 3.1.7 透明度

- 提供"透明度"设置，通过**滑块拖动或数值输入**调节，范围 **0%~100%**（步进 1%），默认 **100%**。
- 透明度作用于进度条整体（**含备注文字**）：灰底、彩色段、边框、指针、起止时间标注与备注文字统一受透明度影响。
- 透明度为 0% 时进度条完全不可见，但相关逻辑照常运行。

### 3.2 指针

- **含义与运动**：标记当前时刻，随时间**平滑连续移动**（不按分钟跳动）。
- **外观**：小尺寸图标，默认使用内置图标（见 §5.4 素材规格）；支持用户替换为自定义图片，按进度条实例生效。
- **方向**：内置图标**垂直于进度条**指向（横放：上/下；竖放：左/右），默认横放朝上、竖放朝左（与备注文字同侧，指针与文字间预留间距避免遮挡）；指向方向可在"指针"分区配置；镜像后指向方向对应反转。用户自定义图片**始终保持图片原始方向显示**，不旋转、不随镜像翻转。
- **缩放**：指针显示尺寸可调（建议 16 ~ 32px）。

### 3.3 时间段备注

#### 3.3.1 定义

一条备注 = 时间段（起始 hh:mm → 结束 hh:mm，24 小时制，精确到分钟）+ 文案 + 颜色。

**同一条进度条上时间段不允许重叠。** 重叠判定：两个时间段 [s1, e1] 与 [s2, e2] 存在 `s1 < e2 且 s2 < e1` 即视为重叠；仅首尾相接（如 11:00-13:00 与 10:00-11:00，在 11:00 相接）**不算重叠，允许**。

**冲突处理交互**：新增/编辑备注时若与已有时间段重叠，弹出提示"与已有时间段重合！是否删除下列冲突备注？"，列出全部冲突备注（时间段 + 文案），用户选择：
- **确认**：删除列出的冲突备注，随后完成新备注的添加/保存；
- **取消**：放弃本次操作，已有备注不变。

#### 3.3.2 彩色段呈现

- 对应时间段涂为**彩色段**（圆头、与进度条同宽、同边框样式），覆盖在灰色底上。
- 颜色：默认从色池取色（规则见 §3.4），同一备注固定使用该色，不反复换色；用户可改为自定义颜色。

#### 3.3.3 备注文字显示

- **触发与替换**：指针位于某时间段内时，显示该时间段的备注文案；指针离开该时间段后，文字**替换**为指针当前所在时间段的备注文案；若指针不在任何时间段内，则不显示任何文字（留空）。
- 由于同一条进度条上时间段不重叠，任意时刻至多只有一条备注生效，不存在多条文字同时显示的情况。
- **位置**：文字位置 = **基准方向（上方 / 下方 / 左侧 / 右侧）+ 拖拽偏移量**叠加。基准方向可配置；偏移量在位置编辑模式中拖拽调整（见 §3.1.5），**切换基准方向时偏移量保留**（仅更换锚点侧）。默认：横放 → 上方；竖放 → **左侧**，偏移量为 0。
- **排列方向**：文字支持**横排（从左到右）**与**竖排（从上到下）**两种排列，可配置；默认横排。
- **样式**：文字颜色可自定义；文字边框可选开启，边框颜色可自定义，边框宽度默认 1px。

#### 3.3.4 时间段起止时间标注

- 当指针位于某时间段内时，在进度条上该时间段的**起始位置**和**结束位置**分别显示其起始时间和结束时间（如 11:00 / 13:00）。
- 时间标注尺寸小但清晰可读（建议 10px），颜色固定为中性灰，初版不做样式配置；指针不在任何时间段内时不显示。
- 当两个时间标注互相重叠或贴近屏幕边缘时，自动缩小字号、错开偏移或收入边界内侧，保证不遮挡、不溢出屏幕。

#### 3.3.5 管理方式

设置窗口内以**表单列表**维护：每行包含起始时间、结束时间、文案、颜色，支持新增、编辑、删除、排序；任何新增/编辑都执行重叠校验（§3.3.1）。

### 3.4 色池

- 内置一套"可爱但不刺眼"的柔和色系（见 §5.2）。
- **随机取色（系统色池）**：时间段**随机取色**时，在色池尚未被全部用完之前，**已用过的颜色不允许复用**——每条新备注随机取一个尚未使用过的颜色；当色池颜色全部用过后，重新开始允许复用。
- **手动选色不受限**：无论是**手动从系统预置色池选色**，还是**使用自定义颜色**，均不受禁止复用限制，允许与已用颜色相同；手动选定/自定义的颜色不进入"未使用颜色"统计，不占用色池配额。仅"随机取色"且色池尚未用完时，才不允许复用已有颜色。
- **删除回归**：一条备注被删除后，该备注随机取用的颜色**回归"未使用"池**，可再次被随机取用。

### 3.5 多实例

- 可创建多条进度条，互不干扰，各自拥有独立配置（方向、镜像、尺寸、位置、置顶、备注、指针、边框等）。
- 每条进度条可命名，可独立显示/隐藏。
- **隐藏即停**：隐藏某条进度条后，该进度条的一切逻辑（指针移动、备注触发、时间标注等）**全部停止运行**，不消耗任何资源；重新显示时才恢复运行。整体保持轻量。
- 所有进度条的时间段**每日自动重复生效**。

### 3.6 运行行为

- **每日重置**：每天 00:00 进度条自动回到全灰初始态，指针归位到 0:00；彩色段、备注与时间标注按当天配置重新生效（备注无需重设，每天重复）。
- **仅关注今天**：不做历史记录，不做统计报表，不跨天累计。
- **开机自启**：默认关闭；用户可在设置中手动开启（开启后 Windows 登录时静默启动到托盘）。

### 3.7 托盘图标

- 程序常驻系统托盘。
- **左键单击**：打开设置窗口。
- **右键菜单**：
  - 新建进度条
  - 进入位置编辑模式（调整位置）
  - 打开设置
  - 退出
- **退出不做二次确认**：点击"退出"立即退出程序；再次启动自动恢复上次全部配置。

### 3.8 设置窗口

#### 3.8.1 标签页（窗口顶部）

- 每个进度条实例对应一个**标签页**，类似浏览器标签页：
  - 标签页显示进度条名称；**左键点击切换**；
  - 标签栏最右侧有 **"+" 按钮，左键点击新建**一条进度条；
  - **右键点击标签页**弹出菜单：重命名、删除（删除前弹确认提示，防止误删）、复制（复制该实例的完整配置为新实例）。
- 进度条名称的默认值：新建时按"进度条 1、进度条 2…"自动编号，并且创建时自动进入重命名状态。

#### 3.8.2 左侧导航栏

当前实例的配置按分区组织在左侧导航栏中：

| 分区 | 内容 |
| --- | --- |
| 外观 | **显示进度条**（显示/隐藏开关）、方向（横/竖）、镜像开关、长度、宽度、边框开关 + 边框颜色、置顶开关、指针大小、**透明度**（滑块 + 数值输入，0%~100%） |
| 位置 | 预设位置、目标显示器选择、坐标输入与微调、**进入位置编辑模式**（拖动调整） |
| 指针 | 图标预览（默认图标，可替换）/ 自定义图片选择、指向方向（横放：上/下；竖放：左/右） |
| 时间段（即"时间段备注"，见 §3.3） | 表单列表（新增 / 编辑 / 删除 / 排序），含重叠校验 |
| 备注文字样式 | 基准方向（上/下/左/右）、排列方向（横排/竖排）、文字颜色、文字边框开关 + 边框颜色 |
| 其它 | 恢复当前实例的默认配置（执行前弹确认提示） |

#### 3.8.3 颜色选择弹窗

- 色池不设独立页面。用户在任何需要选颜色的地方（彩色段、边框、文字、文字边框）点击颜色时，弹出**颜色选择弹窗**：
  1. **系统预设色池**：展示内置色池的全部颜色（色块 + 色值），点击即选；
  2. **自定义**：提供**色盘**（自由取色）与**颜色代码输入框**（直接输入 HEX 色值）；
  3. 自定义得到的颜色即刻可用并保存为自定义颜色，同时记入该弹窗的“最近使用颜色”（最多 10 个，点击可直接选中）。该弹窗为**通用颜色选择组件**，用于备注颜色、边框颜色、文字颜色、文字边框颜色等所有选色场景。

#### 3.8.4 全局标签页

- 全局配置以独立标签页呈现（位于标签栏开头）：开机自启开关、版本信息等。
- **右键点击全局标签页无任何反应**（它不是可修改的进度条实例）。

所有修改**即时生效并自动保存**。

---

## 4. 交互与操作流程

1. **首次运行**：托盘出现图标；自动创建“进度条1”，屏幕底部默认出现一条横放、灰色、居中的进度条。托盘气泡提示"右键托盘图标开始配置"。
2. **配置进度条**：在设置中命名、设置外观、位置等。
3. **添加备注**：设置窗口 → 标签页选择进度条 → 左侧"时间段" → 新增 → 填 11:00、13:00、文案"干饭时间到！" → 系统从色池自动取色（用户也可自定义颜色）。用户点击确定后，进度条上 11:00-13:00 出现彩色段。
4. **备注冲突**：再新增 12:00-13:30"午休" → 弹出冲突提示，列出"干饭时间到！"（11:00-13:00），用户选择删除与否。
5. **备注浮现与时间标注**：指针走进 11:00-13:00，进度条上方浮现"干饭时间到！"，进度条上 11:00 与 13:00 位置显示起止时间；13:00 后文字消失或替换为下一时间段的备注。
6. **拖动调位置**：托盘右键或设置"位置"→ 进入位置编辑模式 → 拖拽进度条 / 备注文字 → Esc 或"完成"退出，恢复鼠标穿透。
7. **日常使用**：只观察进度条，不碰任何设置。

---

## 5. 视觉与配色规范

### 5.1 设计基调

极简、柔和、可爱但克制：**低饱和、中高明度**，马卡龙 / 奶油色系。色值遵循 Pastel 定义（饱和度 ≤ 40%、明度 ≥ 75%），不过于鲜艳，也不阴沉。

- 进度条默认灰底：#E3E0DB（暖灰）。
- 备注文字 / 指针 / 时间标注默认色：#5A5A5A（石板灰）。

### 5.2 内置色池（10 色）

色池综合参考公开 Pastel 配色体系（Pixelbag 50 Pastel Palettes、Figma Pastel Palettes、Venngage Pastel Color Palettes 等）选取，明度集中、饱和度相近，任意相邻 / 任意组合均协调：

| 色名 | 色值 | 观感 |
| --- | --- | --- |
| 蜜桃粉 | #F7C9BE | 柔和的暖桃粉 |
| 玫瑰粉 | #E3C4C4 | 低饱和的玫瑰粉 |
| 杏橙 | #F5C29E | 温润的杏色 |
| 奶油黄 | #FBF0C2 | 奶油般的淡黄 |
| 薄荷绿 | #C9EDE1 | 清爽的浅薄荷 |
| 鼠尾草绿 | #B9C7A0 | 沉静的鼠尾草绿 |
| 天蓝 | #CFE7F5 | 晴空淡蓝 |
| 雾蓝 | #A9D3EA | 温柔的雾蓝 |
| 薰衣草 | #D9CBF2 | 温和的浅紫 |
| 丁香紫 | #C4B3E0 | 淡粉紫（长春花色） |

> 色值为初版建议，最终以实际屏幕观感调试为准。

### 5.3 默认样式速览

| 元素 | 默认值 |
| --- | --- |
| 进度条尺寸 | 600 × 4px，圆头，长度与 24 小时等比例映射 |
| 默认位置 | 屏幕底部居中（横放） |
| 透明度 | 100% |
| 灰底 | #E3E0DB |
| 边框 | 关闭 |
| 置顶 | 开启 |
| 指针 | 内置水滴箭头图标（见 §5.4），16px，横放朝上、竖放朝左（与备注文字同侧），平滑移动 |
| 备注文字 | 12px，深灰 #5A5A5A；横放在上方、竖放在左侧；横排；无文字边框 |
| 时间段起止时间标注 | 10px，中性灰，仅指针位于时间段内时显示 |

### 5.4 指针素材与规格（初版）

- **推荐素材**：Feather Icons 的 "navigation" 图标（水滴箭头造型，MIT 协议，免费可商用无需署名），或 Tabler Icons 同类导航箭头图标；也可在阿里巴巴 iconfont 搜索"水滴 / 箭头"等关键词挑选。
- **规格**：SVG / 透明底 PNG，逻辑尺寸 24 × 24px，运行时按设定尺寸缩放（16 ~ 32px）；默认颜色 #5A5A5A。
- 自定义图片替换在**首版即可用**（见 §3.2、§3.8.2"指针"分区）；更多指针素材与批量管理见 §7 后续规划。

---

## 6. 规则与边界约定

| 场景 | 约定 |
| --- | --- |
| 时间段重叠 | 同一条进度条内不允许重叠；仅首尾相接（端点相同）允许 |
| 跨午夜时间段（如 23:00-01:00） | 起始必须 < 结束，跨午夜请拆成两段设置 |
| 备注文字显示 | 任一时刻至多显示一条（指针所在时间段的文案）；不在任何时间段内则留空 |
| 色池取色 | 随机取色在色池用尽前不重复使用；手动选色（预置色池 / 自定义）不受限制；删除备注后颜色回归未使用池 |
| 隐藏进度条 | 逻辑完全停止，不消耗资源；重新显示后恢复 |
| 00:00 整点 | 进度条瞬时重置，指针归位 |
| 调整长度 | 时间与时间段映射按比例缩放 |
| 透明度 | 0%~100%，默认 100%，作用于进度条整体（含备注文字）；0% 时完全不可见但逻辑照常运行 |
| 数据范围 | 仅"今天"，不区分日期、不跨天累计 |

---

## 7. 首版范围与后续规划

**首版不做（明确排除）：**

- 导出 / 导入配置（未来版本可能支持，本次不做）
- 历史记录、统计报表、跨日视图
- 提醒 / 系统通知（备注仅静态显示，不弹通知、不响铃）
- 周期 / 工作日配置（备注统一每天重复）
- 多语言（初版仅中文界面）
- 主题系统
- 移动端 / 跨平台
- 整点 / 半点刻度线
- 指针旁显示当前时间文字

**后续规划（候选）：**

- 配置导出 / 导入（JSON）
- 自定义指针 / 图标素材的批量管理
- 时间标注与备注文字的样式开放配置

<Window x:Class="Progressing.Windows.ConflictDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="时间段冲突" Width="420" Height="300"
        WindowStartupLocation="CenterOwner" ShowInTaskbar="False" ResizeMode="NoResize"
        Background="#F7F6F3">
    <Grid Margin="16">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Text="与已有时间段重合！是否删除下列冲突备注？"
                   FontWeight="SemiBold" TextWrapping="Wrap" Margin="0,0,0,10" />

        <ListBox x:Name="ConflictList" Grid.Row="1" BorderBrush="#D8D5CC">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="2">
                        <TextBlock Text="{Binding Range}" Foreground="#5A5A5A" FontWeight="SemiBold" />
                        <TextBlock Text="{Binding Text, StringFormat='  {0}'}" Foreground="#333" />
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <StackPanel Grid.Row="2" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,12,0,0">
            <Button Content="取消" Width="80" Height="28" Margin="0,0,8,0" Click="Cancel_Click" />
            <Button Content="确认删除" Width="90" Height="28" IsDefault="True" Click="Ok_Click" />
        </StackPanel>
    </Grid>
</Window>
